import React, { Component } from 'react';
import { ExtButton } from '@sencha/ext-react-modern';

export default class User extends Component {
  render() {
    return (
      <ExtButton text="user" shadow="true"></ExtButton>
    )
  }
};