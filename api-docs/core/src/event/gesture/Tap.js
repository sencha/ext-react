/**
 * @class Ext.event.gesture.Tap
 * @extend Ext.event.gesture.SingleTouch
 * A simple event recogniser which knows when you tap.
 */

/**
 * @cfg {Number} [moveDistance=8]
 * The maximum distance in pixels a touchstart event can travel and still be
 * considered a tap event.
 * @accessor
 */
