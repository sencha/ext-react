/**
 * @class Ext.data.validator.Date
 * @extend Ext.data.validator.Format
 * @alias data.validator.date
 *
 * Validates that the value is a valid date.
 */

/**
 * @cfg {String} [message="Is not a valid date"]
 * The error message to return when the value is not a valid date
 * @accessor
 */

/**
 * @cfg {RegExp} matcher
 * A matcher to check for simple dates. This may be overridden.
 * @accessor
 */
