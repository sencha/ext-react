/**
 * @class Ext.fx.animation.Pop
 * @extend Ext.fx.animation.Abstract
 * @alias animation.pop
 * @alias animation.popIn
 * @private
 */

/**
 * @cfg {Boolean} [out=false]
 * True if you want to make this animation pop out, instead of pop in.
 * @accessor
 */
