/**
 * @class Ext.fx.animation.Wipe
 * @extend Ext.fx.Animation
 * @private
 */

/**
 * @cfg {String} [easing='ease-out']
 * Valid values are 'ease', 'linear', ease-in', 'ease-out', 'ease-in-out',
 * or a cubic-bezier curve as defined by CSS.
 */

/**
 * @cfg {String} [direction='right'] The direction of which the slide animates
 * @accessor
 */

/**
 * @cfg {Boolean} [out=false]
 * True if you want to make this animation wipe out, instead of slide in.
 * @accessor
 */
