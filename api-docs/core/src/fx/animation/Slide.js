/**
 * @class Ext.fx.animation.Slide
 * @extend Ext.fx.animation.Abstract
 * @alias animation.slide
 * @alias animation.slideIn
 * @private
 */

/**
 * @cfg [easing='auto']
 * @inheritdoc
 * @accessor
 */

/**
 * @cfg {Number} [offset=0]
 * The offset that the animation should go offscreen before entering (or when exiting)
 * @accessor
 */

/**
 * @cfg {Boolean} [out=false]
 * True if you want to make this animation slide out, instead of slide in.
 * @accessor
 */

/**
 * @cfg {String} [direction='left]
 * The direction of which the slide animates
 * @accessor
 */
