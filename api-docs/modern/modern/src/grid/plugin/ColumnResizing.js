/**
 * @class Ext.grid.plugin.ColumnResizing
 * @extend Ext.Component
 * @alias plugin.columnresizing
 * @alias plugin.gridcolumnresizing
 *
 * The Column Resizing plugin allows users to adjust the width of the grid columns to suit
 * their needs.  It is automatically added to all grid columns.
 */

/**
 * @cfg {Boolean} [realtime=false]
 * When true the whole column will resize in real-time as the user drags. When false only
 * the header will resize until the interaction is done.
 * @accessor
 */
