/**
 * @class Ext.pivot.plugin.configurator.Panel
 * @extend Ext.Container
 * @alias widget.pivotconfigpanel
 * This class implements the configurator panel.
 */

/**
 * @cfg {String} [panelAllFieldsText='Drop Unused Fields Here']
 * Text displayed in the container reserved for all available fields
 * when docked to top or bottom.
 */

/**
 * @cfg {String} [panelAllFieldsTitle='All fields']
 * Text displayed in the container reserved for all available fields
 * when docked to left or right.
 */

/**
 * @cfg {String} [panelTopFieldsText='Drop Column Fields Here']
 * Text displayed in the container reserved for all top axis fields
 * when docked to top or bottom.
 */

/**
 * @cfg {String} [panelTopFieldsTitle='Column labels']
 * Text displayed in the container reserved for all top axis fields
 * when docked to left or right.
 */

/**
 * @cfg {String} [panelLeftFieldsText='Drop Row Fields Here']
 * Text displayed in the container reserved for all left axis fields
 * when docked to top or bottom.
 */
/**
 * @cfg {String} [panelLeftFieldsTitle='Row labels']
 * Text displayed in the container reserved for all left axis fields
 * when docked to left or right.
 */

/**
 * @cfg {String} [panelAggFieldsText='Drop Agg Fields Here']
 * Text displayed in the container reserved for all aggregate fields
 * when docked to top or bottom.
 */

/**
 * @cfg {String} [panelAggFieldsTitle='Values']
 * Text displayed in the container reserved for all aggregate fields
 * when docked to left or right.
 */
