{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "increment": "lerna publish --skip-git --skip-npm --force-publish",
    "clean": "lerna clean",
    "install:clean": "npm cache clear --force && lerna clean && npm install",
    "kitchensink": "cd packages/ext-react-kitchensink && npm start",
    "boilerplate": "cd packages/ext-react-boilerplate && npm start"
  },
  "devDependencies": {
    "lerna": "~3.2.1"
  }
}
